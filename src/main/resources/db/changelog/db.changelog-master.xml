<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
        Master Changelog — includes all child changelogs in execution order.
        Each file covers a specific category of schema operations.
    -->

    <!-- 1. Initialize Baseline Schema -->
    <include file="db/changelog/001-init-schema.xml"/>

    <!-- 
      ====================================================================
      DEVELOPER INSTRUCTIONS: SQL-FIRST WORKFLOW
      ====================================================================
      All future schema migrations MUST use raw SQL files wrapped in XML.
      Do NOT use Liquibase-native tags (<createTable>, <addColumn>, etc.)
      
      To create a new migration:
      1. Run the scaffold tool (make sure to include master registration):
         `./mvnw exec:java@scaffold -Dscript.name="your-change-name" -Ddomain="your_domain" -Dcontexts="dev,uat,prd" -Dlabels="feature-123"`
      2. This will automatically generate the SQL and XML files, and 
         register the new XML wrapper in this file automatically.
      3. Write your standard BigQuery SQL in the generated `.sql` files.
      ====================================================================
    -->
    <include file="db/changelog/20260309-062137-rename-customer-active-flag.xml"/>
</databaseChangeLog>
